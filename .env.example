# Copier ce fichier en .env.local et remplir.
# Supabase > Project Settings > API
SUPABASE_URL=
# Clé secrète : serveur uniquement, ne jamais préfixer par NEXT_PUBLIC_
SUPABASE_SERVICE_ROLE_KEY=
# Sel pour hacher les IP (rate limit anti-spam). Une chaîne aléatoire longue.
IP_HASH_SALT=
